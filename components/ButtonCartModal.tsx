import { Button, HStack, Heading } from '@chakra-ui/react'
import React from 'react'

type Props = {}

const ButtonCartModal = (props: Props) => {
  return (
    <HStack>
        <Button></Button>
        <Heading>{}</Heading>
        <Button></Button>
    </HStack>
  )
}

export default ButtonCartModal