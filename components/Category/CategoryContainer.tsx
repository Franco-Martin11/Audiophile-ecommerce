import { Stack, Text } from "@chakra-ui/react";
import React from "react";

type Props = {};

function CategoryContainer(props: Props): JSX.Element {
  return (
    <Stack>
      <Text>CategoryContainer</Text>
    </Stack>
  );
}

export default CategoryContainer;
