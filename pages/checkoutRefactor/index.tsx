import CheckoutForm from "@/components/CheckoutRefactor";
import React from "react";

type Props = {};

const CheckoutRefactor = (props: Props) => {
  return <CheckoutForm />;
};

export default CheckoutRefactor;
